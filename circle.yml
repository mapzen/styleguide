machine:
  node:
    version: 4.2.6

dependencies:
  post:
    - npm install -g gulp-cli && gulp build

deployment:
  production:
    branch: master
    commands:
      - gulp publish --target dev
      - gulp publish --target prod

general:
  artifacts:
    - "dist"
